export const navButtonStyles = (bgColor, textColor) => ({
  my: 2,
  marginLeft: { xs: "55px" },
  display: "block",
  textTransform: "none",
  fontSize: {
    lg: "1.2rem"
  },

  backgroundColor: bgColor,
  color: textColor
});

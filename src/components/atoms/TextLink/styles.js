const styles = {
  text(fontStyle, fontSize) {
    return {
      color: "links.main",
      textDecoration: "underline",
      fontStyle: fontStyle,
      fontSize: fontSize
    };
  },
};

export default styles;

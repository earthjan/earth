import { globalSpacing } from "../../../../theme/defaultTheme";

const styles = {
  root: { marginTop: { xs: "100px" } },
  text: {
    fontSize: "0.8rem",
    textAlign: "center",
  },
  textCredit: {
    fontSize: "0.8rem",
    textAlign: "center",
  },
  logoBase: {
    flexDirection: "row",
    justifyContent: "center",
    columnGap: 1,
    marginTop: globalSpacing.MarginSpacing,
  },
  logo: {
    height: "24px",
    width: "24px",
  }
};

export default styles;
